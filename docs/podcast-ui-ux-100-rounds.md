# Podcast-first UI/UX：100 輪優化紀錄

日期：2026-07-11

基準版本：`f563128`

初始架構輪（Round 0）：首頁書籍改連 Podcast；`/books/<slug>` 成為單頁播放器；`/books/<slug>/read` 保留完整電子書。

## 第 1–25 輪：資訊架構與播放核心

1. [x] 首頁書籍封面改為 Podcast 主入口。
2. [x] 首頁主 CTA 改為「開始收聽／繼續收聽」。
3. [x] 書籍主路由改為 Podcast-first。
4. [x] 新增獨立電子書 `/read` 路由。
5. [x] 電子書頁新增返回 Podcast 入口。
6. [x] Podcast 頁保留返回書庫入口。
7. [x] Podcast 頁提供顯著但次要的「閱讀電子書」。
8. [x] Podcast 與電子書共用穩定 book slug。
9. [x] 沿用 v3 localStorage，不破壞既有進度。
10. [x] 恢復進度但不強制自動播放。
11. [x] 播放中每秒保存 Podcast 進度。
12. [x] 暫停時立即保存。
13. [x] 頁面隱藏時立即保存。
14. [x] `pagehide` 時立即保存。
15. [x] `beforeunload` 時立即保存。
16. [x] Client-side 路由卸載時立即保存。
17. [x] 重新進入時顯示「繼續 Podcast」。
18. [x] 進度條支援直接跳轉。
19. [x] 同時顯示已播與剩餘時間。
20. [x] 顯示百分比或「已保存，可繼續」。
21. [x] 提供 1x–2.5x 播放速度。
22. [x] 播放速度持久化。
23. [x] 自動下一集預設開啟並持久化。
24. [x] 顯示下一集標題與連播狀態。
25. [x] 每集保留下載入口。

## 第 26–50 輪：單頁層級與響應式

26. [x] Podcast 根節點固定為 `100dvh`。
27. [x] 禁止整頁垂直捲動。
28. [x] 禁止整頁水平溢出。
29. [x] 桌機採封面／播放器雙欄層級。
30. [x] 行動版改為緊湊書籍列／播放器上下層級。
31. [x] 放大主播放鍵並置於控制中心。
32. [x] 上一集與下一集置於控制列兩端。
33. [x] 加入倒退 15 秒。
34. [x] 加入快進 30 秒。
35. [x] 首尾集正確停用不存在的切換按鈕。
36. [x] 單集選擇使用緊湊下拉選單。
37. [x] 單集標題限制兩行避免推擠。
38. [x] 摘要限制兩行降低閱讀負擔。
39. [x] 手機隱藏次要摘要以保留操作空間。
40. [x] 手機封面改為橫向資訊卡。
41. [x] 1440×900 無捲動、無重疊。
42. [x] 1366×768 無捲動、無重疊。
43. [x] 768×1024 無捲動、無重疊。
44. [x] 390×844 無捲動、無重疊。
45. [x] 360×640 無捲動、無重疊。
46. [x] 844×390 橫向模式無捲動、無重疊。
47. [x] 六種視窗的核心控制皆保持可見。
48. [x] 加入瀏海與安全區 inset。
49. [x] 低高度時隱藏 footer／次要資訊。
50. [x] 橫向低高度改用專屬緊湊控制尺寸。

## 第 51–75 輪：可及性與狀態回饋

51. [x] 使用 `main/header/aside/section/footer` 語意結構。
52. [x] 播放控制加入 `role="group"`。
53. [x] 播放設定加入 `role="group"`。
54. [x] 自動下一集使用原生 switch 語意。
55. [x] 載入狀態提供 `aria-busy`。
56. [x] 播放狀態使用 `aria-live`。
57. [x] 錯誤訊息使用 `role="alert"`。
58. [x] Range 提供動態 `aria-label`。
59. [x] Range 提供時間格式 `aria-valuetext`。
60. [x] 所有圖示按鈕提供可讀名稱。
61. [x] 所有封面圖片提供替代文字。
62. [x] Range 支援鍵盤方向鍵。
63. [x] Range 方向鍵不會誤切換集數。
64. [x] 單集選擇保留原生鍵盤操作。
65. [x] 播放速度保留原生鍵盤操作。
66. [x] Range 加入高辨識焦點框。
67. [x] Select 加入高辨識焦點框。
68. [x] 主要連結與按鈕維持全域焦點框。
69. [x] 停用按鈕以視覺和語意同步呈現。
70. [x] 播放中狀態使用綠色但不只依賴顏色。
71. [x] 錯誤狀態使用文字與紅色邊框雙重提示。
72. [x] 載入時顯示旋轉指示器。
73. [x] 無音頻書籍提供明確空狀態。
74. [x] 無音頻空狀態提供直接閱讀入口。
75. [x] 電子書頁移除重複的完整 Podcast 面板。

## 第 76–100 輪：續播、效能與視覺精修

76. [x] Podcast 寫入時保留電子書閱讀欄位。
77. [x] 電子書寫入時保留 Podcast 欄位。
78. [x] Media Session 同步書名、集名與作者。
79. [x] Media Session 支援播放。
80. [x] Media Session 支援暫停。
81. [x] Media Session 支援倒退。
82. [x] Media Session 支援快進。
83. [x] Media Session 支援指定時間跳轉。
84. [x] Media Session 支援上一集／下一集。
85. [x] Media Session 同步 position state。
86. [x] 音頻只預載 metadata，避免整檔預抓。
87. [x] 音頻引擎保持隱藏但使用原生 `<audio>`。
88. [x] 封面使用 Next Image 與響應式 sizes。
89. [x] 視覺層級限制為綠、琥珀與中性色。
90. [x] 主播放鍵依播放狀態切換色彩。
91. [x] 封面狀態點只在播放中脈動。
92. [x] 支援 `prefers-reduced-motion`。
93. [x] 首頁顯示 Podcast 續播集數與秒數。
94. [x] 首頁 Podcast 書架優先於分類書架。
95. [x] 首頁焦點卡說明在手機限制四行。
96. [x] 電子書書籤降為 Podcast 之後的次要資訊。
97. [x] React hooks、cleanup、語意與 key 完成品質檢查。
98. [x] `lint` 與 Next.js production build 通過第一輪。
99. [x] 建立可重跑的 `npm run verify:podcast-ux` 六視窗驗收。
100. [x] 完整 E2E 通過：90 秒續播、2x、鍵盤跳轉、選第 2 集、電子書路由與空狀態。

## 驗收摘要

- 所有 Podcast 視窗：無整頁捲動、無水平溢出、無互動控制重疊。
- 六個測試尺寸共顯示 12–13 個核心控制。
- 瀏覽器 console 與 Next.js error overlay：0。
- 永久驗收指令：`PODCAST_BASE_URL=http://127.0.0.1:3001 npm run verify:podcast-ux`。
